# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: bazzite-float
# description will be included in the image's metadata
description: A bit opinionated distro made by Float. Bazzite DX GNOME on the latest stream.
alt-tags:
  - latest

# the base image to build on top of (FROM) and the version tag to use
base-image: ghcr.io/ublue-os/bazzite-dx-gnome
image-version: latest # Bazzite publishes only stream tags (no numeric 44/43)

# module configuration, executed in order
# you can include multiple instances of the same module
modules:
  - from-file: features/system-files.yml
  - from-file: features/application-rpms.yml
  - from-file: features/cockpit.yml
  - from-file: features/fonts.yml
  - from-file: features/gnome-ui-fixes.yml
  - from-file: features/wallpapers.yml
  - from-file: features/flatpaks.yml
  - type: files
    files:
    - source: dconf-bluefin
      destination: /etc/dconf/db/distro.d
  - type: gnome-extensions
    install:
      - Dash to Dock
  - type: script
    snippets: []
    scripts: 
      - ublue-disable-steam-autostart.sh
  - type: script
    env:
      UBLUE_WALLPAPER: pixel-pusher-l.jxl
    scripts:
      - set-default-wallpaper.sh