# yaml-language-server: $schema=https://schema.blue-build.org/module-stage-list-v1.json
modules:
  - type: dnf
    repos:
      copr:
        - ublue-os/packages
    install:
      packages:
        - f39-backgrounds-base

  - type: script
    scripts:
      - install-distro-wallpapers.sh
