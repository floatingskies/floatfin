# The socket starts very early (sockets.target); never let it fire the daemon
# before /nix has been bind-mounted onto persistent /var storage.
[Unit]
Requires=floatfin-nix.mount
After=floatfin-nix.mount